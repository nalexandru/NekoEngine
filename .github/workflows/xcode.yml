name: Xcode

on:
  push:
    branches: [ master ]
    
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-11.0

    steps:
      - uses: actions/checkout@v2

      - name: Build
        working-directory: ${{github.workspace}}
        run: xcodebuild

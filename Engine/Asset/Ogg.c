//#include <stb_vorbis.h>

#include <Engine/IO.h>
#include <Engine/Asset.h>

bool
E_LoadOggAsset(struct Stream *stm, struct AudioClip *clip)
{
	/*int channels, sampleRate;
	short *pcm;*/

/*	if (stb_vorbis_decode_memory(data, (int)dataSize, &channels, &sampleRate, &pcm) < 0)
		return false;

	return true;*/
	return false;
}
